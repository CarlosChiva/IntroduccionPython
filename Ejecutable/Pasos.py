"""Para windows abrir cmd y escribir:
pip install pyinstaller


Una vez echa la instalacion.
accedemos al fichero delarchivo principal con:
cd

Utilizamos el comando
pyinstaller --windowed --onefile Archivo_principal.py
pyinstaller sirve para ordenar la instalacion del programa
--windowed sirve para eliminar la terminal a la hora de ejecutar el programa
--onefile sirve para que en la carpeta(dist) donde se encuentra el ejecutable resulte un solo
elemento ejecutable(un poco mas pesado)
"""